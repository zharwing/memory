{
  "name": "@aimem/cli",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "aimem": "dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@aimem/api-client": "workspace:*"
  }
}
