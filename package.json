{
  "name": "@aimem/workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Local-first project context manager for AI-assisted coding tools.",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev:daemon": "node --env-file-if-exists=.env --import tsx apps/daemon/src/index.ts",
    "dev:cli": "node --env-file-if-exists=.env --import tsx apps/cli/src/index.ts",
    "dev:mcp": "node --env-file-if-exists=.env --import tsx apps/mcp-server/src/index.ts",
    "dev:web": "vite --config apps/desktop/vite.config.ts",
    "dev:desktop": "node scripts/tauri-dev.mjs",
    "build": "tsc -b && vite build --config apps/desktop/vite.config.ts",
    "typecheck": "tsc -b",
    "test": "node scripts/run-tests.mjs"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.0"
  }
}
