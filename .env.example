# Local Zharwing Memory configuration.
# Copy this file to .env and choose the profile and private store path for your machine.

ZHARWING_MEMORY_HOST=127.0.0.1
ZHARWING_MEMORY_PORT=37841
# Use token for the hardened local daemon. Personal-preview may explicitly use
# none on loopback, but that profile is not a security boundary.
ZHARWING_MEMORY_AUTH_MODE=token
# Personal-preview browser development requires both explicit profile flags
# and loopback-only ZHARWING_MEMORY_AUTH_MODE=none. Hardened-local never uses
# this preview session path.
# ZHARWING_MEMORY_PROFILE=personal-preview
# ZHARWING_PUBLIC_PROFILE=personal-preview

# Enable the authenticated MCP daily-memory surface for Codex and other agents.
ZHARWING_MEMORY_AGENT_SURFACE=enabled

# Optional desktop-shell overrides. Canonical names win; legacy AIMEM_* names
# remain readable for compatibility with existing local installations.
ZHARWING_MEMORY_DESKTOP_AUTOSTART_DAEMON=true
# ZHARWING_MEMORY_DESKTOP_PROJECT_ROOT=<absolute-path-to-app-source>
# ZHARWING_MEMORY_DESKTOP_DAEMON_COMMAND=<custom-daemon-command>

# Private local data. This folder stores projects, sessions, docs, imports,
# context bundles, inbox proposals, and backups. Do not commit it.
ZHARWING_MEMORY_ROOT=<absolute-path-to-private-ai-memory-store>

ZHARWING_MEMORY_DAEMON_URL=http://127.0.0.1:37841
# No credential value belongs in this template. The daemon creates a bounded
# credential in the OS user-state directory unless a trusted runtime supplies
# one through its private launch environment.
# Hardened agent mode additionally requires the trusted launcher to supply a
# distinct project-bound agent credential; it must not be copied into this file.

# Browser UI receives only explicitly public, allowlisted runtime values.
# Authentication uses a single-use bootstrap exchange, HttpOnly cookie, and
# memory-only CSRF value; no bearer credential is a browser build input.
ZHARWING_PUBLIC_DAEMON_URL=http://127.0.0.1:37841
